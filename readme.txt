Para ejecutar el programa solo se necesita compilarlo.
Una vez ejecutado, porfavor inserte el nodo en el cual se quiere iniciar.

El archivo graph.txt tiene:
-Numero de nodos (ciudades)
-El grafo de restricciones utilizado (la matriz de adyacencia)